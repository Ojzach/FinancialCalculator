<UserControl x:Class="FinancialCalculator.Views.DepositConfirmationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900">
    <Grid>
        <DockPanel>
            <!-- Header -->
            <Border DockPanel.Dock="Top" Background="#F0F0F0" BorderBrush="#CCCCCC" BorderThickness="0,0,0,1" Padding="16,12">
                <StackPanel>
                    <TextBlock Text="Confirm Deposit" FontSize="22" FontWeight="SemiBold"/>
                    <TextBlock FontSize="14" Margin="0,4,0,0">
                        <Run Text="Total Deposit: "/>
                        <Run Text="{Binding TotalDeposit, StringFormat=c}" FontWeight="SemiBold"/>
                        <Run Text="    Take-Home: "/>
                        <Run Text="{Binding TotalTakeHome, StringFormat=c}" FontWeight="SemiBold"/>
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- Footer buttons -->
            <Border DockPanel.Dock="Bottom" Background="#F8F8F8" BorderBrush="#CCCCCC" BorderThickness="0,1,0,0" Padding="16,10">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="I have made all the transfers above." VerticalAlignment="Center" Margin="0,0,12,0" Foreground="#555"/>
                    <Button Content="← Back" Padding="12,5" Margin="0,0,8,0"
                            Command="{Binding BackCommand}"/>
                    <Button Content="Confirm &amp; Finalize" Padding="12,5" Background="#4CAF50" Foreground="White"
                            Command="{Binding ConfirmCommand}"/>
                </StackPanel>
            </Border>

            <!-- Main content -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
                <StackPanel>
                    <!-- Budget account transfers -->
                    <TextBlock Text="Budget Allocations" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>

                    <ItemsControl ItemsSource="{Binding AccountGroups}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="4" Margin="0,0,0,10" Padding="12">
                                    <StackPanel>
                                        <!-- Account header -->
                                        <DockPanel>
                                            <TextBlock FontWeight="SemiBold" FontSize="14">
                                                <Run Text="{Binding AccountName}"/>
                                                <Run Text=" — "/>
                                                <Run Text="{Binding InstitutionName}" Foreground="#666"/>
                                            </TextBlock>
                                            <TextBlock HorizontalAlignment="Right" FontWeight="SemiBold" FontSize="14"
                                                       Text="{Binding TotalAmount, StringFormat=c}"/>
                                        </DockPanel>

                                        <!-- Budget breakdown -->
                                        <ItemsControl ItemsSource="{Binding Items}" Margin="16,6,0,0">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <DockPanel Margin="0,2">
                                                        <TextBlock Text="{Binding BudgetName}" Foreground="#444"/>
                                                        <TextBlock HorizontalAlignment="Right" Foreground="#444"
                                                                   Text="{Binding Amount, StringFormat=c}"/>
                                                    </DockPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- Deductions -->
                    <TextBlock Text="Payroll Deductions" FontSize="16" FontWeight="SemiBold" Margin="0,12,0,8"/>
                    <Border BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="4" Padding="12">
                        <ItemsControl ItemsSource="{Binding Deductions}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <DockPanel Margin="0,3">
                                        <TextBlock Text="{Binding BudgetName}" Foreground="#444"/>
                                        <TextBlock HorizontalAlignment="Right" Foreground="#444"
                                                   Text="{Binding Amount, StringFormat=c}"/>
                                    </DockPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </DockPanel>
    </Grid>
</UserControl>
