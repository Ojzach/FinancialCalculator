<UserControl x:Class="FinancialCalculator.Views.DepositCalculatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FinancialCalculator.Views"
             xmlns:model="clr-namespace:FinancialCalculator.ViewModels"
             xmlns:converters="clr-namespace:FinancialCalculator.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ResourceDictionaries/DepositCalculatorDictionary.xaml" />
                <ResourceDictionary Source="/ResourceDictionaries/BudgetEditPanelDictionary.xaml"/>

                <ResourceDictionary>
                    <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <DockPanel>

            <Grid Margin="5, 40, 5, 25" DockPanel.Dock="Top">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition MaxWidth="150"/>
                    <ColumnDefinition MaxWidth="75"/>
                    <ColumnDefinition MaxWidth="75"/>

                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="25"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" Grid.Column="0" Grid.Row="0">
                    <TextBlock Text="Estimated Yearly Income: "/>
                    <TextBox Text="{Binding EstimatedYearlyIncome, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Width="100"/>
                </StackPanel>

                <TextBlock Text="{Binding MonthsCoveredByPaycheck, StringFormat='Months Covered By Paycheck Modifier: {0}'}" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Left"/>

                <TextBlock Text="Paycheck" Grid.Column="1" Grid.Row="0"/>
                <TextBox  Text="{Binding PaycheckAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="3" Grid.Row="0" Margin="3"/>

                <TextBlock Text="Federal Income Tax" Grid.Row="1" Grid.Column="1"/>
                <TextBox Text="{Binding FederalTaxPct, Converter={StaticResource PercentConverter}, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="2" Margin="3"/>
                <TextBox Text="{Binding FederalTaxAmt, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="3" Margin="3"/>

                <TextBlock Text="Medicare" Grid.Row="2" Grid.Column="1"/>
                <TextBox Text="{Binding MedicarePct, Converter={StaticResource PercentConverter}, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="2" Margin="3"/>
                <TextBox Text="{Binding MedicareAmt, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="3" Margin="3"/>

                <TextBlock Text="Social Security" Grid.Row="3" Grid.Column="1"/>
                <TextBox Text="{Binding SocialSecurityPct, Converter={StaticResource PercentConverter}, UpdateSourceTrigger=PropertyChanged}" Grid.Row="3" Grid.Column="2" Margin="3"/>
                <TextBox Text="{Binding SocialSecurityAmt, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Row="3" Grid.Column="3" Margin="3"/>

                <TextBlock Text="State Income Tax" Grid.Row="4" Grid.Column="1"/>
                <TextBox Text="{Binding StateTaxPct, Converter={StaticResource PercentConverter}, UpdateSourceTrigger=PropertyChanged}" Grid.Row="4" Grid.Column="2" Margin="3"/>
                <TextBox Text="{Binding StateTaxAmt, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Row="4" Grid.Column="3" Margin="3"/>

            </Grid>

            <Grid DockPanel.Dock="Bottom" Margin="25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition MaxWidth="150"/>
                    <ColumnDefinition MaxWidth="75"/>

                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="25"/>

                </Grid.RowDefinitions>

                <Button Content="Submit" Grid.Column="1" Grid.Row="1"/>
            </Grid>

            <ScrollViewer HorizontalAlignment="Stretch" VerticalAlignment="Stretch" VerticalScrollBarVisibility="Auto">
                <ContentPresenter VerticalAlignment="Stretch" ContentTemplate="{StaticResource DepositBudget}" Content="{Binding DepositBudgets}" Margin="10, 0, 50, 0"/>
            </ScrollViewer>



        </DockPanel>

        <Grid Visibility="{Binding IsEditPanelOpen, Converter={StaticResource BoolToVisibilityConverter}}" d:Visibility="Collapsed">
            <Border Background="Gray" Opacity="0.5"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="4*"/>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>

                <Border Background="White" Grid.Row="1" Grid.Column="1" BorderThickness="3" BorderBrush="Gray" CornerRadius="4" Padding="10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition Height="25"/>
                        </Grid.RowDefinitions>

                        <ContentControl Content="{Binding CurrentlyEditingBudget}" ContentTemplate="{StaticResource BaseBudget_EditPanelTemplate}" Grid.Row="0"/>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>



                            <Button Content="Close" Command="{Binding CloseEditMenuCommand}" Grid.Column="1"/>
                        </Grid>

                    </Grid>
                </Border>

            </Grid>

        </Grid>
    </Grid>
    
</UserControl>
