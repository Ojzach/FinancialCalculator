<UserControl x:Class="FinancialCalculator.Views.DepositCalculatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FinancialCalculator.Views"
             xmlns:model="clr-namespace:FinancialCalculator.ViewModels"
             xmlns:converters="clr-namespace:FinancialCalculator.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ResourceDictionaries/DepositCalculatorDictionary.xaml" />
                <ResourceDictionary Source="/ResourceDictionaries/BudgetEditPanelDictionary.xaml"/>

                <ResourceDictionary>
                    <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition MinHeight="100"/>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>

            <Grid Margin="10, 10, 15, 10" Grid.Row="0">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition/>
                    <ColumnDefinition MaxWidth="75"/>
                    <ColumnDefinition MaxWidth="75"/>

                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" Grid.Column="0" Grid.Row="0">
                    <TextBlock Text="Estimated Yearly Income: "/>
                    <TextBox Text="{Binding EstimatedYearlyIncome, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Width="100"/>
                </StackPanel>

                <TextBlock Text="{Binding MonthsCoveredByPaycheck, StringFormat='Months Covered By Paycheck Modifier: {0}'}" Grid.Column="1" HorizontalAlignment="Left"/>

                <TextBlock Text="Paycheck" Grid.Column="2"/>
                <TextBox Text="{Binding PaycheckAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="3"/>

            </Grid>

            <ScrollViewer Grid.Row="1" Width="300" Margin="10, 10, 10, 10" HorizontalAlignment="Right" VerticalAlignment="Stretch" VerticalScrollBarVisibility="Auto">
                <StackPanel Orientation="Vertical" Margin="3">
                    <ListBox VerticalAlignment="Stretch" BorderThickness="0"  ItemsSource="{Binding DepositDeductions}">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0, 2, 0, 2"/>
                                <Setter Property="BorderThickness" Value="0"/>
                            </Style>
                        </ListBox.ItemContainerStyle>

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Height="25">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition Width="75"/>
                                        <ColumnDefinition Width="75"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="{Binding BudgetName}"/>

                                    <Border BorderBrush="Green" Grid.Column="1" Margin="3">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers >
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding DepositIsUsrSet}" Value="true"/>
                                                            <Condition Binding="{Binding DepositIsSetByAmt}" Value="false"/>
                                                        </MultiDataTrigger.Conditions>

                                                        <Setter Property="BorderThickness" Value="1"/>
                                                    </MultiDataTrigger>
                                                    <DataTrigger Binding="{Binding DepositIsUsrSet}" Value="false">
                                                        <Setter Property="BorderThickness" Value="0"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding DepositIsSetByAmt}" Value="true">
                                                        <Setter Property="BorderThickness" Value="0"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBox Text="{Binding UsrDepositPct, Converter={StaticResource PercentConverter}, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Border>

                                    <Border BorderBrush="Green" Grid.Column="2" Margin="3">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers >
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding DepositIsUsrSet}" Value="true"/>
                                                            <Condition Binding="{Binding DepositIsSetByAmt}" Value="true"/>
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="BorderThickness" Value="1"/>
                                                    </MultiDataTrigger>
                                                    <DataTrigger Binding="{Binding DepositIsUsrSet}" Value="false">
                                                        <Setter Property="BorderThickness" Value="0"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding DepositIsSetByAmt}" Value="false">
                                                        <Setter Property="BorderThickness" Value="0"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBox Text="{Binding UsrDepositAmt, StringFormat='c', UpdateSourceTrigger=PropertyChanged}"/>
                                    </Border>

                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>

                        <ListBox.Template>
                            <ControlTemplate TargetType="ListBox">
                                <ScrollViewer Focusable="false" CanContentScroll="true" Padding="20, 2, 0, 2">
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </ControlTemplate>
                        </ListBox.Template>

                    </ListBox>
                    
                    
                </StackPanel>
                
                
            </ScrollViewer>

            <ScrollViewer Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" VerticalScrollBarVisibility="Auto">
                <StackPanel Orientation="Vertical" Margin="3">

                    <Border Background="LightGray" BorderBrush="Gray" BorderThickness="2" Height="30" HorizontalAlignment="Stretch">

                        <Grid  HorizontalAlignment="Stretch">
                            <Grid.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="TextAlignment" Value="Left"/>
                                </Style>
                                <Style TargetType="TextBox">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="TextAlignment" Value="Center"/>
                                </Style>
                            </Grid.Resources>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition MinWidth="150"/>
                                <ColumnDefinition MaxWidth="300" Width="10*"/>
                                <ColumnDefinition Width="75"/>
                                <ColumnDefinition Width="75"/>
                                <ColumnDefinition MaxWidth="30"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Take Home Amount" FontWeight="Bold" Grid.Column="0" Margin="5, 0, 0, 0"/>

                            <TextBox Text="{Binding TakeHomePercent, Converter={StaticResource PercentConverter}, Mode=OneWay}" IsEnabled="False" Grid.Column="2" Margin="3"/>

                            <TextBox Text="{Binding TakeHomeAmount, StringFormat='c', Mode=OneWay}" IsEnabled="False" Grid.Column="3" Margin="3"/>

                        </Grid>
                    </Border>


                    <ListBox VerticalAlignment="Stretch" BorderThickness="0"  ItemsSource="{Binding DepositBudgets}" ItemTemplate="{DynamicResource DepositBudget}">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0, 2, 0, 2"/>
                                <Setter Property="BorderThickness" Value="0"/>
                            </Style>
                        </ListBox.ItemContainerStyle>

                        <ListBox.Template>
                            <ControlTemplate TargetType="ListBox">
                                <ScrollViewer Focusable="false"
                          CanContentScroll="true"
                          Padding="20, 2, 0, 2">
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </ControlTemplate>
                        </ListBox.Template>

                    </ListBox>

                    <Border Background="LightGray" BorderBrush="Gray" BorderThickness="2" Height="30" Margin="20, 2, 0, 2" HorizontalAlignment="Stretch" ToolTipService.InitialShowDelay="100">

                        <Grid>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="175"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Unallocated Take Home Amount" FontWeight="Bold" Grid.Column="0" Margin="5, 0, 0, 0" HorizontalAlignment="Left"/>

                            <Grid Grid.Column="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="75"/>
                                    <ColumnDefinition Width="75"/>
                                </Grid.ColumnDefinitions>

                                <TextBox Text="{Binding UnallocatedTakeHomePercent, Converter={StaticResource PercentConverter}, Mode=OneWay}" IsEnabled="False" Grid.Column="0" Margin="3"/>

                                <TextBox Text="{Binding UnallocatedTakeHomeAmount, StringFormat='c', Mode=OneWay}" IsEnabled="False" Grid.Column="1" Margin="3"/>
                            </Grid>

                        </Grid>
                    </Border>

                </StackPanel>
            </ScrollViewer>

            <Grid Grid.Row="3" Margin="25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition MaxWidth="150"/>
                    <ColumnDefinition MaxWidth="75"/>

                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="25"/>

                </Grid.RowDefinitions>

                <Button Content="Submit" Grid.Column="1" Grid.Row="1"/>
            </Grid>

        </Grid>

        <Grid Visibility="{Binding IsEditPanelOpen, Converter={StaticResource BoolToVisibilityConverter}}" d:Visibility="Collapsed">
            <Border Background="Gray" Opacity="0.5"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="4*"/>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>

                <Border Background="White" Grid.Row="1" Grid.Column="1" BorderThickness="3" BorderBrush="Gray" CornerRadius="4" Padding="10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition Height="25"/>
                        </Grid.RowDefinitions>

                        <ContentControl Content="{Binding CurrentlyEditingBudget}" ContentTemplate="{StaticResource BaseBudget_EditPanelTemplate}" Grid.Row="0"/>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>



                            <Button Content="Close" Command="{Binding CloseEditMenuCommand}" Grid.Column="1"/>
                        </Grid>

                    </Grid>
                </Border>

            </Grid>

        </Grid>
    </Grid>
    
</UserControl>
