<UserControl x:Class="FinancialCalculator.Views.DepositCalculatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FinancialCalculator.Views"
             xmlns:model="clr-namespace:FinancialCalculator.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ResourceDictionaries/DepositCalculatorDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
        
    </UserControl.Resources>

    <Grid>

        <ScrollViewer Margin="100, 0, 0, 0" HorizontalAlignment="Stretch" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="10, 10, 50, 10" Orientation="Vertical">
                <TextBlock Height="40"/>

                <Grid Margin="5">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition MaxWidth="150"/>
                        <ColumnDefinition MaxWidth="75"/>
                        <ColumnDefinition MaxWidth="75"/>

                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="25"/>
                        <RowDefinition Height="25"/>
                        <RowDefinition Height="25"/>
                        <RowDefinition Height="25"/>
                        <RowDefinition Height="25"/>
                        <RowDefinition Height="25"/>

                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Grid.Column="0" Grid.Row="0">
                        <TextBlock Text="Estimated Yearly Income: "/>
                        <TextBox Text="{Binding EstimatedYearlyIncome, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Width="100"/>
                    </StackPanel>

                    <TextBlock Text="{Binding MonthsCoveredByPaycheck, StringFormat='Months Covered By Paycheck Modifier: {0}'}" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Left"/>

                    <TextBlock Text="Paycheck" Grid.Column="1" Grid.Row="0"/>
                    <TextBox  Text="{Binding PaycheckAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="3" Grid.Row="0" Margin="3"/>

                    <TextBlock Text="Federal Income Tax" Grid.Row="1" Grid.Column="1"/>
                    <TextBox Text="{Binding MonthlyPctStr, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="2" Margin="3"/>
                    <TextBox Text="{Binding MonthlyAmt, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="3" Margin="3"/>

                    <TextBlock Text="Medicare" Grid.Row="2" Grid.Column="1"/>
                    <TextBox Text="{Binding MonthlyPctStr, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="2" Margin="3"/>
                    <TextBox Text="{Binding MonthlyAmt, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="3" Margin="3"/>

                    <TextBlock Text="Social Security" Grid.Row="3" Grid.Column="1"/>
                    <TextBox Text="{Binding MonthlyPctStr, UpdateSourceTrigger=PropertyChanged}" Grid.Row="3" Grid.Column="2" Margin="3"/>
                    <TextBox Text="{Binding MonthlyAmt, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Row="3" Grid.Column="3" Margin="3"/>

                    <TextBlock Text="State Income Tax" Grid.Row="4" Grid.Column="1"/>
                    <TextBox Text="{Binding MonthlyPctStr, UpdateSourceTrigger=PropertyChanged}" Grid.Row="4" Grid.Column="2" Margin="3"/>
                    <TextBox Text="{Binding MonthlyAmt, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Row="4" Grid.Column="3" Margin="3"/>


                    <TextBlock Text="Take Home Amount" Grid.Column="1" Grid.Row="5"/>
                    <TextBlock Text="{Binding TakeHomePercent, StringFormat='{}Used: {0:P2}'}" Grid.Column="2" Grid.Row="5"/>
                    <TextBlock Text="{Binding TakeHomeAmount, StringFormat='c'}" Grid.Column="3" Grid.Row="5"/>

                </Grid>

                <ContentPresenter Height="400" ContentTemplate="{StaticResource DepositBudget}"
                  Content="{Binding DepositBudgets}"/>

                <!--<ListBox ItemsSource="{Binding BalanceSheets}" ItemTemplateSelector="{Binding Source={StaticResource BalanceSheetTemplateSelector}}">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>

-->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition MaxWidth="150"/>
                        <ColumnDefinition MaxWidth="75"/>

                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="25"/>

                    </Grid.RowDefinitions>

                    <TextBlock Text="Unused Amount: " Grid.Column="1" Grid.Row="0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding UnusedAmount, StringFormat='c'}" Grid.Column="2" Grid.Row="0" VerticalAlignment="Center"/>

                    <Button Content="Submit" Grid.Column="1" Grid.Row="1"/>
                </Grid>

            </StackPanel>
        </ScrollViewer>
        
        
    </Grid>
</UserControl>
