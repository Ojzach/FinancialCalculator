<UserControl x:Class="FinancialCalculator.Views.FinancialAccountsPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FinancialCalculator.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ResourceDictionaries/FinancialAccountsDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>

    </UserControl.Resources>


    <Grid>


        
        <ScrollViewer Margin="100, 0, 0, 0" HorizontalAlignment="Stretch" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="10, 10, 50, 10" Orientation="Vertical">


                <TextBlock Height="40" Text="Financial Accounts" FontSize="25" FontWeight="DemiBold"/>


                <ListBox ItemsSource="{Binding FinancialInstitutions}" SelectedItem="{Binding SelectedFinancialInstitution}" ItemTemplate="{StaticResource lbFinancialInstitution}">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Delete" Command="{Binding DeleteFinancialInstitutionCommand}" />
                        </ContextMenu>
                    </ListBox.ContextMenu>
                </ListBox>

                <Grid Margin="5" Visibility="{Binding AddFinancialInstitutionVisible}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Name:" Grid.Column="0"/>
                    <TextBox Text="{Binding AddFinancialInstitutionName, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1"/>
                    <Button Content="Submit" Command="{Binding AddFinancialInstitutionCommand}" Grid.Column="2"/>
                </Grid>

                <Button Content="Add" Command="{Binding ToggleAddBoxCommand}" Margin="0, 5, 0, 0"/>

            </StackPanel>
        </ScrollViewer>

        <Grid Visibility="{Binding EditAccountVisibility}" d:Visibility="Collapsed">
            <Border Background="Gray" Opacity="0.5"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="4*"/>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>

                <Border Background="White" Grid.Row="1" Grid.Column="1" BorderThickness="3" BorderBrush="Gray" CornerRadius="4" Padding="10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition Height="25"/>
                        </Grid.RowDefinitions>

                        <ContentControl Content="{Binding CurrentlyEditingAccount}" ContentTemplate="{StaticResource FinancialAccountEditBox}" Grid.Row="0"/>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>

                            

                            <Button Content="Close" Command="{Binding CloseEditMenuCommand}" Grid.Column="1"/>
                        </Grid>
                        
                    </Grid>
                </Border>
                
            </Grid>

        </Grid>
    </Grid>

</UserControl>
