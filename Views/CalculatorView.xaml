<UserControl x:Class="FinancialCalculator.View.CalculatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FinancialCalculator.View"
             xmlns:model="clr-namespace:FinancialCalculator.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">

        <ScrollViewer Margin="100, 0, 0, 0" HorizontalAlignment="Stretch" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="10, 10, 50, 10" Orientation="Vertical">
                <TextBlock Height="40"/>

                <Grid Height="25" Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition MaxWidth="75"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Paycheck" Grid.Column="0"/>
                    <TextBox Grid.Column="1" Text="{Binding PaycheckAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>

                <Grid Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition MaxWidth="75"/>
                        <ColumnDefinition MaxWidth="75"/>

                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="20"/>

                    </Grid.RowDefinitions>

                    <TextBlock Text="Total Tax" Grid.Column="0" Grid.Row="0"/>
                    <TextBlock Text="{Binding TotalTaxPercent, StringFormat='P2'}" Grid.Column="1" Grid.Row="0"/>
                    <TextBlock Text="{Binding TotalTaxAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="2" Grid.Row="0"/>

                    <TextBlock Text="Federal Tax" Grid.Column="0" Grid.Row="1"/>
                    <TextBlock Text="{Binding FederalTaxPercent, StringFormat='P2'}" Grid.Column="1" Grid.Row="1"/>
                    <TextBox Text="{Binding FederalTaxAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="2" Grid.Row="1"/>

                    <TextBlock Text="State Tax" Grid.Column="0" Grid.Row="2"/>
                    <TextBlock Text="{Binding StateTaxPercent, StringFormat='P2'}" Grid.Column="1" Grid.Row="2"/>
                    <TextBox Text="{Binding StateTaxAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="2" Grid.Row="2"/>

                </Grid>

                <Grid Height="25" Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition MaxWidth="75"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Take Home Amount" Grid.Column="0"/>
                    <TextBlock Grid.Column="1" Text="{Binding TakeHomeAmount, StringFormat='c'}"/>
                </Grid>

                <ListBox ItemsSource="{Binding BalanceSheets}">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical">

                                <Grid Margin="5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                        <ColumnDefinition MaxWidth="75"/>
                                        <ColumnDefinition MaxWidth="75"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="{Binding BalanceSheetName}" Grid.Column="0"/>
                                    <TextBlock Text="Notes" Grid.Column="1"/>
                                    <TextBlock Text="{Binding TotalBalanceSheetPercent, StringFormat='P2'}" Grid.Column="2"/>
                                    <TextBlock Text="{Binding TotalBalanceSheetAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="3"/>
                                </Grid>

                                <ListBox Height="150" ItemsSource="{Binding BalanceSheetItems}">
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem">
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Grid HorizontalAlignment="Stretch">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition/>
                                                    <ColumnDefinition/>
                                                    <ColumnDefinition MaxWidth="75"/>
                                                    <ColumnDefinition MaxWidth="75"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Text="{Binding Name}" Grid.Column="0"/>
                                                <TextBox Text="{Binding Notes}" Grid.Column="1"/>
                                                <TextBox Text="{Binding MonthlyPercentStr, UpdateSourceTrigger=PropertyChanged}" Grid.Column="2"/>
                                                <TextBox Text="{Binding MonthlyAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="3"/>

                                            </Grid>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>

                                </ListBox>

                                <Button Content="Add"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

<!--
                <Grid Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition MaxWidth="75"/>
                        <ColumnDefinition MaxWidth="75"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Total Savings" Grid.Column="0"/>
                    <StackPanel Orientation="Horizontal" Grid.Column="1">
                        <TextBlock Text="Max Percentage: "/>
                        <TextBox Text="{Binding BalanceSheet.MaxTotalSavingsPercentStr, UpdateSourceTrigger=PropertyChanged}" Width="75"/>
                    </StackPanel>
                    <TextBlock Text="{Binding BalanceSheet.TotalSavingsPercent, StringFormat='P2'}" Grid.Column="2" Grid.Row="2"/>
                    <TextBlock Text="{Binding BalanceSheet.TotalSavingsAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="3" Grid.Row="2"/>
                </Grid>

                <ListBox Height="100" ItemsSource="{Binding BalanceSheet.SavingsBalanceItems}">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition MaxWidth="75"/>
                                    <ColumnDefinition MaxWidth="75"/>
                                    <ColumnDefinition MaxWidth="75"/>
                                    <ColumnDefinition MaxWidth="75"/>
                                    <ColumnDefinition MaxWidth="100"/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{Binding Name}" Grid.Column="0"/>
                                <TextBox Text="{Binding MonthlyPercentStr, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1"/>
                                <TextBox Text="{Binding MonthlyAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="2"/>
                                <TextBox Text="{Binding GoalSavingsAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="3"/>
                                <TextBox Text="{Binding CurrentSavingsAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="4"/>
                                <DatePicker SelectedDate="{Binding GoalDate}" Grid.Column="5"/>
                                <TextBox Text="{Binding Notes}" Grid.Column="6"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                -->

            </StackPanel>
        </ScrollViewer>
        
        
    </Grid>
</UserControl>
