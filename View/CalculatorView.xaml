<UserControl x:Class="FinancialCalculator.View.CalculatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FinancialCalculator.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">

        <ScrollViewer Width="500" Margin="100, 0, 0, 0" HorizontalAlignment="Left" VerticalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical" Margin="10">
                <TextBlock Height="40"/>

                <Grid Height="25" Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Paycheck" Grid.Column="0"/>
                    <TextBox Grid.Column="1" Text="{Binding BalanceSheet.PaycheckAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>

                <Grid Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>

                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="20"/>

                    </Grid.RowDefinitions>

                    <TextBlock Text="Total Tax" Grid.Column="0" Grid.Row="0"/>
                    <TextBlock Text="{Binding BalanceSheet.TotalTaxPercent, StringFormat='P2'}" Grid.Column="1" Grid.Row="0"/>
                    <TextBlock Text="{Binding BalanceSheet.TotalTaxAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="2" Grid.Row="0"/>

                    <TextBlock Text="Federal Tax" Grid.Column="0" Grid.Row="1"/>
                    <TextBlock Text="{Binding BalanceSheet.FederalTaxPercent, StringFormat='P2'}" Grid.Column="1" Grid.Row="1"/>
                    <TextBox Text="{Binding BalanceSheet.FederalTaxAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="2" Grid.Row="1"/>

                    <TextBlock Text="State Tax" Grid.Column="0" Grid.Row="2"/>
                    <TextBlock Text="{Binding BalanceSheet.StateTaxPercent, StringFormat='P2'}" Grid.Column="1" Grid.Row="2"/>
                    <TextBox Text="{Binding BalanceSheet.StateTaxAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="2" Grid.Row="2"/>

                </Grid>

                <Grid Height="25" Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Take Home Amount" Grid.Column="0"/>
                    <TextBlock Grid.Column="1" Text="{Binding BalanceSheet.TakeHomeAmount, StringFormat='c'}"/>
                </Grid>


                <Grid Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Total Investment" Grid.Column="0" Grid.Row="2"/>
                    <TextBlock Text="{Binding BalanceSheet.TotalInvestmentPercent, StringFormat='P2'}" Grid.Column="1" Grid.Row="2"/>
                    <TextBlock Text="{Binding BalanceSheet.TotalInvestmentAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="2" Grid.Row="2"/>
                </Grid>

                <ListBox Height="100" ItemsSource="{Binding BalanceSheet.InvestmentBalanceItems}">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{Binding Name}" Grid.Column="0"/>
                                <TextBox Text="{Binding MonthlyPercentStr, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1"/>
                                <TextBox Text="{Binding MonthlyAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="2"/>
                                <TextBox Text="{Binding Notes}" Grid.Column="3"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>

                </ListBox>


                <Grid Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Total Fixed Costs" Grid.Column="0" Grid.Row="2"/>
                    <TextBlock Text="{Binding BalanceSheet.TotalFixedCostsPercent, StringFormat='P2'}" Grid.Column="1" Grid.Row="2"/>
                    <TextBlock Text="{Binding BalanceSheet.TotalFixedCostsAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="2" Grid.Row="2"/>
                </Grid>

                <ListBox Height="100" ItemsSource="{Binding BalanceSheet.FixedCostsBalanceItems}">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{Binding Name}" Grid.Column="0"/>
                                <TextBox Text="{Binding MonthlyPercentStr, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1"/>
                                <TextBox Text="{Binding MonthlyAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="2"/>
                                <TextBox Text="{Binding Notes}" Grid.Column="3"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>

                </ListBox>


                <Grid Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Total Savings" Grid.Column="0" Grid.Row="2"/>
                    <TextBlock Text="{Binding BalanceSheet.TotalSavingsPercent, StringFormat='P2'}" Grid.Column="1" Grid.Row="2"/>
                    <TextBlock Text="{Binding BalanceSheet.TotalSavingsAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="2" Grid.Row="2"/>
                </Grid>

                <ListBox Height="100" ItemsSource="{Binding BalanceSheet.SavingsBalanceItems}">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{Binding Name}" Grid.Column="0"/>
                                <TextBox Text="{Binding MonthlyPercentStr, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1"/>
                                <TextBox Text="{Binding MonthlyAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="2"/>
                                <TextBox Text="{Binding GoalSavingsAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="3"/>
                                <TextBox Text="{Binding CurrentSavingsAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="4"/>
                                <DatePicker SelectedDate="{Binding GoalDate}" Grid.Column="5"/>
                                <TextBox Text="{Binding Notes}" Grid.Column="6"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>


                <Grid Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Total Free Spending" Grid.Column="0" Grid.Row="2"/>
                    <TextBlock Text="{Binding BalanceSheet.TotalFreeSpendingPercent, StringFormat='P2'}" Grid.Column="1" Grid.Row="2"/>
                    <TextBlock Text="{Binding BalanceSheet.TotalFreeSpendingAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="2" Grid.Row="2"/>
                </Grid>

                <ListBox Height="100" ItemsSource="{Binding BalanceSheet.FreeSpendingBalanceItems}">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{Binding Name}" Grid.Column="0"/>
                                <TextBox Text="{Binding MonthlyPercentStr, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1"/>
                                <TextBox Text="{Binding MonthlyAmount, StringFormat='c', UpdateSourceTrigger=PropertyChanged}" Grid.Column="2"/>
                                <TextBox Text="{Binding Notes}" Grid.Column="3"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>

                </ListBox>

            </StackPanel>
        </ScrollViewer>
        
        
    </Grid>
</UserControl>
